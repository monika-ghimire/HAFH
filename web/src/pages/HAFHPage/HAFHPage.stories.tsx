import type { Meta, StoryObj } from '@storybook/react'

import HafhPage from './HafhPage'

const meta: Meta<typeof HafhPage> = {
  component: HafhPage,
}

export default meta

type Story = StoryObj<typeof HafhPage>

export const Primary: Story = {}
